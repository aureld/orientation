// Re-export from new location — this file will be removed once all imports are updated
export {
  DIMENSIONS,
  RADAR_PAIRS,
  emptyProfile,
  type Dimension,
  type ProfileVector,
} from "@/domain/profile";
